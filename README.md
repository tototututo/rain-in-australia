# 🌧️ Прогнозування Дощу в Австралії (ML Deployment with Streamlit)

## Опис Проекту

Цей проект демонструє повний цикл **Машинного Навчання (MLOps)**, від навченої моделі до інтерактивного веб-додатку. Тут розгорнута модель **Логістичної Регресії** (навчену на даних про погоду в Австралії) за допомогою фреймворку **Streamlit**.

Додаток дозволяє користувачам вводити дані про погоду за сьогодні (температура, вологість, швидкість вітру тощо) та отримувати прогноз ймовірності того, чи піде дощ завтра (`RainTomorrow`).


## ⚙️ Технологічний Стек

- **Мова:** Python

- **ML Фреймворки:** `scikit-learn` (Logistic Regression, Imputer, Scaler, Encoder)

- **Веб-Фреймворк:** `Streamlit`

- **Серіалізація:** `joblib`

- **Дані:** `weatherAUS.csv` (дані про погоду в Австралії)

## 📂 Структура Репозиторію

```
/rain-in-australia
├── data
    ├── weatherAUS.csv       # Набір даних на якиї навчалась модель
├── images                   # Директорія для зображень які використовуються у застосунку
├── models                    
    ├── aussie_rain.joblib       # Серіалізований об'єкт, що містить:
    │                            # - Навчену модель (LogisticRegression)
    │                            # - SimpleImputer, MinMaxScaler, OneHotEncoder
    │                            # - Списки колонок для препроцесингу
├── app.py                   # Основний код Streamlit-додатку та інтерфейсу.
├── requirements.txt         # Список необхідних Python-бібліотек.
└── README.md                # Цей файл.
```

## 🚀 Локальний Запуск
Щоб запустити додаток локально, виконайте наступні кроки:

### 1. Клонування репозиторію

  ```bash
    git clone https://github.com/tototututo/rain-in-australia.git
    cd rain-in-australia
   ```

### 2. Встановлення залежностей
Створіть та активуйте віртуальне середовище (рекомендовано), а потім встановіть необхідні бібліотеки:

  ```bash
    pip install -r requirements.txt
   ```

### 3. Запуск Streamlit-додатку
Переконайтеся, що файл aussie_rain.joblib знаходиться у кореневій папці проекту, а потім запустіть додаток:

  ```bash
    streamlit run app.py
   ```

Додаток відкриється локально у вашому браузері за замовчуванням.


## 📊 Моделювання

При натисканні кнопки "Прогнозувати" виконується наступний потік даних:
1. **Введення даних:** Користувач вводить погодні параметри через інтерфейс Streamlit.
2. **Десеріалізація:** Завантажується об'єкт `aussie_rain.joblib`.
3. **Препроцесинг:**
  - **Імпутація:** Пропущені значення (якщо були б у вхідних даних) замінюються середніми значеннями, на яких було навчено `SimpleImputer`.
  - **Масштабування:** Числові ознаки масштабуються до діапазону $[0, 1]$ за допомогою навченого `MinMaxScaler`.
  - **Кодування:** Категоріальні ознаки (наприклад, `Location`, `WindGustDir`) перетворюються на числові вектори за допомогою навченого `OneHotEncoder`.
4. **Прогнозування:** Підготовлені дані подаються на вхід моделі **Logistic Regression** (`model.predict` та `model.predict_proba`).
5. **Виведення результату:** Користувач отримує остаточний прогноз (**Yes/No**) та відповідну **ймовірність**.


